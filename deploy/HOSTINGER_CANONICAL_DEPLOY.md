# مسار النشر المعتمد على هوستينجر

## الحالة الحالية (2026-08-18)

النطاق `https://smarteditor.bazarvan.com` يعمل على Supabase الذاتي داخل نفس VPS.
مسار التشغيل المعتمد هو:

`/var/www/bazarvan-editor-staging`

أزيلت عمليات PM2 القديمة (`bazarvan-editor` والعمال الستة) التي كانت تستخدم
Supabase Cloud، وحُفظت قائمة PM2 الجديدة عبر `pm2 save`. لا تُشغّل المجلد
`/var/www/bazarvan-editor` أو ملف بيئته القديم؛ فهو أرشيف محلي فقط. جميع مواضع
عبارة **Supabase SQL Editor** في هذا الدليل تعني قاعدة Supabase الذاتية على VPS،
وليس مشروع Cloud.

## ترحيلات التحليل الخارجي

### تأهيل المنافسين من محتوى الصفحة

قبل نشر محرك اختيار المنافسين القائم على ظهور الكلمة الأساسية أو صيغها البديلة، نفّذ مرة واحدة:

`supabase/migrations/20260827020000_competitor_content_qualification.sql`

يضيف الترحيل الصيغ البديلة إلى بصمة جاهزية اكتشاف المنافسين وإلى نسخة إدخال جميع مهام الاكتشاف، دون تخزين نصوص صفحات جديدة أو إضافة جداول.

قبل أول نشر لعامل التحليل الخارجي، نفّذ الترحيلات التالية بالترتيب داخل **Supabase SQL Editor**:

1. `supabase/migrations/20260710000000_external_analysis_foundation.sql`
2. `supabase/migrations/20260710010000_external_analysis_worker_queue.sql`
3. `supabase/migrations/20260710020000_external_semantic_generation.sql`
4. `supabase/migrations/20260710030000_external_engineering_commands.sql`
5. `supabase/migrations/20260711000000_external_analysis_job_controls.sql`
6. `supabase/migrations/20260711010000_dashboard_filtered_pagination.sql`
7. `supabase/migrations/20260711020000_external_analysis_scheduler_settings.sql`
8. `supabase/migrations/20260711030000_external_analysis_command_preferences.sql`
9. `supabase/migrations/20260712000000_external_analysis_independent_batches.sql`
10. `supabase/migrations/20260713000000_phase_0_1_security_hardening.sql`
11. `supabase/migrations/20260713010000_phase_2_3_access_and_atomic_article_save.sql`

نفّذ الترحيلين 10 و11 قبل نشر إصدار الويب والخادم المقابل لهما. تعتمد واجهة حفظ المقالة الجديدة على الدالة `save_article_snapshot` التي يضيفها الترحيل 11.

### إصلاح عدالة طابور المنافسين

إذا كانت الترحيلات السابقة مطبقة، نفّذ الترحيل الجديد التالي مرة واحدة فقط داخل **Supabase SQL Editor**:

`supabase/migrations/20260725000000_external_analysis_queue_fairness.sql`

يمنع هذا الترحيل مهمة مؤجلة لإعادة المحاولة في مقالة واحدة من حجز العامل ومنع مهام المنافسين الجاهزة في المقالات الأخرى. لا يحذف المهام ولا يلغي مواعيد إعادة المحاولة، بل يجعل العامل يتجاوز العمل غير المستحق مؤقتًا ثم يعود إليه عند حلول موعده.

### منع الأوامر الهندسية عند غياب نص المقالة

نفّذ الترحيل التالي مرة واحدة قبل نشر إصدار الخادم المقابل:

`supabase/migrations/20260726010000_external_engineering_article_text_guard.sql`

يجعل هذا الترحيل **نص المقالة المحفوظ** شرطًا إلزاميًا لحزمة الأوامر الهندسية في قاعدة البيانات نفسها. لا ينشئ أي حزمة جديدة للمقالة الفارغة، ويلغي مهامها الهندسية المنتظرة أو الجارية أو المؤجلة الموجودة سابقًا، ولا يرسل طلبًا إلى Gemini. لا يُعاد تشغيل الحزمة إلا بعد كتابة النص وحفظ المقالة واستكمال بقية الشروط.

بعده نفّذ الترحيل الأحدث التالي مرة واحدة قبل نشر الخادم والواجهة:

`supabase/migrations/20260727010000_external_analysis_word_threshold.sql`

يرفع هذا الترحيل شرط حزمة الأوامر الخارجية إلى **أكثر من 100 كلمة محفوظة** (101 كلمة فأكثر) للحزم الافتراضية التي حددها المسؤول وللأوامر المختارة يدويًا. وفي المقابل يفصل توليد الصيغ البديلة وLSI عن نص المحرر، لذلك يمكن تشغيل التوليد الدلالي حتى عندما يكون نص المقالة فارغًا. يعيد الترحيل حساب الجاهزية الحالية ويلغي فقط مهام الأوامر الهندسية النشطة التي لم تعد تحقق شرط الكلمات.

### تقليل استهلاك Supabase وإيقاظ العمال

نفّذ الترحيل التالي مرة واحدة داخل **Supabase SQL Editor** قبل إعادة تحميل عمليات PM2:

`supabase/migrations/20260728020000_worker_queue_wake_signals.sql`

ينشئ الترحيل جدول إشارات صغيرًا فقط، ولا ينشر صفوف الطوابير الكبيرة عبر Realtime. ترسل الطوابير إشارة صغيرة عند وصول عمل جاهز، فيستيقظ العامل فورًا. أثناء خلو الطوابير يستخدم كل عامل مراقبًا واحدًا بانتظار تدريجي حتى 30 ثانية بدل أن يستعلم كل مسار تنفيذ بصورة مستقلة كل بضع ثوانٍ. يبقى الفحص الدوري احتياطيًا، لذلك لا تضيع المهام إذا انقطع Realtime؛ قد يتأخر البدء بحد أقصى 30 ثانية فقط حتى يعود الاتصال.

لا يلزم إضافة متغيرات جديدة إلى `.env.production` لأن ملف PM2 يضع حد الخمول الافتراضي `30000` مللي ثانية. يمكن تخصيصه عند الحاجة بمتغيرات `*_IDLE_MAX_MS` الموثقة في `deploy/env.server.example`. لا تخفض قيمته عن فاصل الفحص الأساسي، ولا ترفعها فوق 60 ثانية.

### زر إنشاء المقالة الشامل

بعد ترحيل إشارات العمال، نفّذ ترحيل الزر الشامل مرة واحدة قبل تحديث الخادم والواجهة:

`supabase/migrations/20260728030000_full_article_pipeline.sql`

ثم نفّذ ترحيل الأمان اللاحق قبل تحديث الخادم والواجهة:

`supabase/migrations/20260824010000_full_article_pipeline_safety.sql`

ثم نفّذ ترحيل المتطلبات الاختيارية قبل نشر الإصدار الذي يسمح بتشغيل الزر من
العنوان ونص المحرر والكلمة الأساسية فقط:

`supabase/migrations/20260824020000_full_article_pipeline_optional_prerequisites.sql`

يجعل هذا الترحيل اسم الشركة والاختيارات اليدوية لسياق الصفحة اختيارية في مسار
الإنشاء الشامل وحده. لا يغيّر شروط زر الكتابة اليدوية أو الطابور التلقائي، ولا
يخترع اسم شركة؛ تمرر الكتابة قيمة صريحة تفيد بأن الشركة غير محددة وتستخدم
الموجز المولد بدل إلزام المستخدم باختيارات يدوية قبل بدء المراحل السبع.

ثم نفّذ ترحيل اكتشاف المنافسين الاختياري للمسار الشامل:

`supabase/migrations/20260824030000_full_article_pipeline_optional_discovery.sql`

ينشئ هذا الترحيل مهمة اكتشاف منافسين مملوكة لمسار الإنشاء الشامل اعتمادًا على
العنوان أو الكلمة الأساسية. ويبقي شرط اسم الشركة كما هو للمسارين اليدوي
والتلقائي، فلا يحدث توسع غير مقصود في صلاحية بقية نقاط البدء.

ثم نفّذ ترحيل التوليد الدلالي الاختياري للمسار الشامل:

`supabase/migrations/20260825000000_full_article_pipeline_optional_semantic.sql`

ينشئ هذا الترحيل تمريرات مستقلة ومملوكة للمنسق لإكمال الصيغ البديلة وLSI مع
فحص ملكية العامل وجيل الـlease. تبقى الشركة والاختيارات اليدوية للسياق
اختيارية لهذا المسار وحده، بينما تظل شروط التوليد اليدوي والتلقائي كما هي.

يضيف الترحيل الأول منسق المراحل السبع وعامل PM2 المستقل. يغيّر ترحيل الأمان التطبيق النهائي ليحدث فقط بعد تحليل المسودة وإعادة فحص الجودة، ويحفظ المسودة للمراجعة عند وجود مانع بدل إدراجها. كما يضيف ربط الأب بالأبناء، وأجيال lease مانعة للعامل القديم، وحدًا دائمًا للمحاولات، وفحص CAS لنسخة المقالة، ويحافظ ذريًا على الصيغ وLSI والموجز التي كتبها العامل عند وصول حفظ تلقائي قديم من المتصفح. إذا كان الترحيل الأول مطبقًا، لا تُعده؛ نفّذ ترحيل الأمان فقط ثم أوامر النشر المعتادة.

## ترحيلات كتابة المقالة المنظمة

قبل نشر نظام كتابة المقالة المنظمة، نفّذ الترحيلات التالية بالترتيب:

1. `supabase/migrations/20260722000000_content_writing_sessions.sql` — يمكن تخطيه فقط إذا سبق تنفيذه بنجاح.
2. `supabase/migrations/20260722010000_structured_content_writing.sql`
3. `supabase/migrations/20260722020000_content_writing_application.sql`
4. `supabase/migrations/20260722030000_content_writing_external_reporting.sql`
5. `supabase/migrations/20260722040000_content_writing_quality_guards.sql`
6. `supabase/migrations/20260723000000_content_writing_quality_policy.sql`
7. `supabase/migrations/20260723010000_content_writing_knowledge_workflow.sql`
8. `supabase/migrations/20260723020000_content_writing_resume_preferences.sql`
9. `supabase/migrations/20260728000000_dynamic_content_writing_final_section.sql`
10. `supabase/migrations/20260728010000_content_writing_faq_independence.sql`
11. `supabase/migrations/20260728040000_content_writing_final_structure.sql`
12. `supabase/migrations/20260812000000_allow_parallel_content_writing_substeps.sql`
13. `supabase/migrations/20260812010000_gemini_key_availability_waiting.sql`
14. `supabase/migrations/20260823010000_automatic_content_writing_queue.sql`
15. `supabase/migrations/20260823020000_content_writing_competitor_preparation.sql`

تضيف هذه الترحيلات جلسات دائمة، وخطوات قابلة للاستئناف، وإدراج المقالة بعد المراجعة، وتسجيل النتائج الخارجية، ومنع تشغيل أكثر من جلسة نشطة، وتقارير جودة حتمية ذات إصدارات، وفهرسة معرفة المنافسين، وتدقيق التغطية، وإصلاح الأقسام المستهدفة، ومرحلة ختامية ديناميكية تستخدم دعوة اتخاذ الإجراء بدل الخاتمة للصفحات الخدمية والتجارية، وتدقيق استقلالية FAQ، وحارسًا يمنع تكرار القسم النهائي ويثبت FAQ قبله مباشرة، وطابور كتابة تلقائية دائمًا يعالج مقالة واحدة ثم يطبق فترة تهدئة قابلة للضبط.

يجب تنفيذ الترحيلات الأربعة عشر كلها قبل نشر إصدار الخادم المقابل. إذا كانت الترحيلات الثلاثة عشر الأولى منفذة، فلا تعِد تنفيذها؛ نفّذ الترحيل الرابع عشر فقط. يضيف الترحيل الرابع عشر فحص الجاهزية والطابور الذري وفترة التهدئة وإعادة المحاولة والتقارير المرئية، ويبقى متوقفًا افتراضيًا حتى يفعله المسؤول. يضيف الترحيل الثالث عشر فحصًا تجميعيًا آمنًا لحالة مفاتيح Gemini، لكي يفرّق المحرك بين المفتاح المحجوز، والمفتاح داخل التبريد، والمفتاح المعطل، والمفتاح المؤهل، وينتظر مدة قصيرة عند الإمكان بدل إعلان نفاد المفاتيح فورًا. لا تعيد الدالة أي قيمة مفتاح أو بصمة، ولا يسمح بتنفيذها إلا لدور الخادم `service_role`. يسمح الترحيل الثاني عشر للقراءات المستقلة ومرشحي الكتابة التابعين للمرحلة نفسها بمشاركة ترتيبها، مع بقاء مفتاح كل خطوة فريدًا، ويزيل خطأ القيد `content_writing_steps_session_id_ordinal_key` دون حذف خطوات الجلسات السابقة. يجعل الترحيل الثامن استئناف الكتابة يعتمد المزود والموديل اللذين يختارهما المستخدم قبل الاستئناف، ويضيف التاسع نوع مرحلة دعوة اتخاذ الإجراء، ويعيد العاشر تدقيق FAQ للجلسات غير المكتملة، ويعيد الحادي عشر القسم النهائي ومراحله التابعة فقط لضمان البنية الجديدة. يعيد فحص الجاهزية `/readyz` حالة HTTP 503 إذا لم يكن مخطط قاعدة البيانات المطلوب أو طابور الكتابة التلقائية أو دالة تنسيق المفاتيح متوفرًا.

## ترحيل سجل الأوامر الهندسية

قبل نشر إصدار تبويب **الأوامر الهندسية** الجديد، نفّذ:

`supabase/migrations/20260724000000_admin_prompt_registry.sql`

ينشئ هذا الترحيل سجل الإعدادات العام الذي يحفظ تعديلات المسؤول على الأوامر. الترحيل آمن عند إعادة تنفيذه: يحافظ على الأوامر المعدلة الموجودة ولا يستبدلها بالقيم الافتراضية. إذا لم تكن قد نشرت هذا الإصدار من قبل، نفّذ الترحيل مرة واحدة ثم أكمل أوامر النشر المعتادة أدناه.

### ترحيل بطاقة موجز المقالة النصي

قبل نشر الإصدار الذي يفصل الموجز المولد عن اختيارات المستخدم، نفّذ مرة واحدة:

`supabase/migrations/20260726060000_generated_content_brief_text_block.sql`

يحدّث هذا الترحيل إصدار سجل الأوامر الهندسية ويحذف فقط النسخة القديمة المحفوظة من أمر **توليد موجز المقالة الذكي** لأنها كانت تُرجع حقولًا وتستبدل اختيارات المستخدم. يستخدم النظام بعد ذلك الأمر الافتراضي الجديد الذي يُرجع نص البطاقة المستقلة، مع الحفاظ على بقية أوامر المسؤول المعدلة. بعد النشر يستطيع المسؤول تعديل الأمر الجديد مجددًا من **الإعدادات ← الأوامر الهندسية ← الكتابة**.

## ترحيلات مركز العملاء

قبل نشر إصدار الخادم الذي يتحقق من جاهزية **مركز العملاء**، نفّذ:

نفّذ بالترتيب:

1. `supabase/migrations/20260724010000_client_center_foundation.sql`
2. `supabase/migrations/20260724020000_client_center_management_and_crawling.sql`
3. `supabase/migrations/20260724030000_internal_linking_engine.sql`
4. `supabase/migrations/20260724040000_client_semantic_index.sql`
5. `supabase/migrations/20260724050000_editor_internal_link_suggestions.sql`
6. `supabase/migrations/20260724060000_internal_link_quality_policies.sql`
7. `supabase/migrations/20260725010000_client_draft_creation.sql`
8. `supabase/migrations/20260728050000_client_page_crawl_source.sql`
9. `supabase/migrations/20260728060000_local_client_site_crawler.sql`
10. `supabase/migrations/20260730010000_crawler_provider_secrets.sql`
11. `supabase/migrations/20260730020000_hybrid_client_site_crawler.sql`
12. `supabase/migrations/20260730030000_crawler_provider_usage_reports.sql`
13. `supabase/migrations/20260730040000_client_page_ai_link_profiles.sql`
14. `supabase/migrations/20260731010000_client_site_crawl_pgcrypto_fix.sql`
15. `supabase/migrations/20260731020000_economic_external_client_crawler.sql`

ينشئ الترحيل الأول السجلات المركزية للعملاء والدومينات وتعيين الموظفين وصفحات الموقع ومهام الزحف. ويضيف الترحيل الثاني حقول الاستخراج ودوال قائمة الزحف الدائمة اللازمة للمرحلتين الثانية والثالثة. ويضيف الترحيل الثالث ربط المقالة بعميل واحد وسجل تطبيق أو تجاهل اقتراحات الربط الداخلي للمرحلتين الرابعة والخامسة. ويضيف الترحيل الرابع قواميس العميل وملفات الفهرس الخوارزمي للمرحلة السادسة، بما فيها الكلمات الموزونة والعبارات والجذور ودرجة اكتمال البيانات. ويضيف الترحيل الخامس رابط الصفحة الحالية وسجل فحوص الاقتراحات وإجراءات المنع والإبلاغ اللازمة لتكامل المرحلة السابعة داخل المحرر. ويضيف الترحيل السادس سياسة جودة عامة قابلة للتخصيص لكل عميل، تشمل حد الدرجة وكثافة الروابط ومنع التكرار وعبارات Anchor Text المستبعدة للمرحلة الثامنة. ويضيف الترحيل السابع إنشاء سجل أولي آمن للعميل. يضيف الترحيلان الثامن والتاسع مصدر الصفحات المكتشفة وتشغيل الزاحف وخريطة الروابط الداخلية. يضيف الترحيل العاشر التخزين المشفّر لمفاتيح Firecrawl وBrowserless، ويضيف الترحيل الحادي عشر اختيار مزوّد الجلب لكل تشغيل والوضع الهجين التلقائي، ويضيف الترحيل الثاني عشر سجل تقارير مفصل لكل محاولة جلب دون تخزين المفتاح الخام. ويضيف الترحيل الثالث عشر ملف عبارات ربط ذكي منفصلًا لكل صفحة مع حالة التوليد والمراجعة والثقة والسياقات السلبية، من دون تخزين HTML أو النص الكامل للصفحة. ويصلح الترحيل الرابع عشر مسار `pgcrypto` داخل Trigger خريطة الروابط حتى تكتمل مهام Browserless وFirecrawl والمحلية بعد حفظ الروابط المستخرجة. ويضيف الترحيل الخامس عشر إعادة استخدام الصفحات الحديثة وشبكة روابطها دون اتصال خارجي، وحدودًا ذرية لكل زحف ولكل مزوّد شهريًا، مع خيار تحديث كامل محصور بالمسؤول.

المرحلة التاسعة نفسها لم تكن تضيف ترحيلًا جديدًا. إذا كنت قد نفذت ترحيلاتها وقتها وترحيل سجل الأوامر الهندسية `20260724000000_admin_prompt_registry.sql`، فلا تُعِد تنفيذها. تضيف تلك المرحلة مراجعة اختيارية بالذكاء الاصطناعي لأفضل نتائج المحرك الخوارزمي، وتكون معطلة افتراضيًا ولا تعمل تلقائيًا. يُقرأ أمرها العربي من **الإعدادات ← الأوامر الهندسية ← الربط الداخلي**، ولا تستطيع المراجعة إضافة صفحة أو رابط أو Anchor Text خارج النتائج والبيانات الموجودة في مركز العميل.

المرحلة العاشرة والأخيرة نفسها لم تكن تضيف ترحيلًا أو أمرًا هندسيًا جديدًا؛ فهي بوابة اختبارات ونشر نهائية. أصبح `npm run build` يشغّل تلقائيًا **13 حالة قبول** ويوقف النشر عند فشل أي منها: الصلاحيات، حصر الروابط بدومين العميل الرئيسي، تنظيف التكرار، التحويلات وCanonical، استبعاد `noindex` و404، العربية والإنجليزية، غياب الوصف، العناوين العامة، دقة Anchor Text، عدم استخدام مقالات المحرر، عمل المحرك دون مفتاح API، حفظ التعديلات اليدوية عند إعادة الزحف، واستئناف مهام الزحف الفاشلة.

مصدر الصفحات الهدف مستقل تمامًا عن مقالات المحرر. يبقى الزحف والفهرس الدلالي الأساسيان برمجيين ويعملان دون مفتاح API أو Search Console، ويضيف الإثراء الاختياري بالذكاء الاصطناعي عبارات بديلة فقط عند توفر مزوّد مفعّل. لا توجد ميزة للصفحات اليتيمة، ولا تتضمن الترحيلات حقول الشعار أو أسلوب الكتابة أو المصطلحات أو الخدمات أو الجمهور أو الأسواق أو ملاحظات المسؤول.

إذا سبق أن نفّذت ترحيلات مركز العملاء السابقة، فلا تعِد تنفيذها؛ نفّذ الترحيلات `20260730010000_crawler_provider_secrets.sql` و`20260730020000_hybrid_client_site_crawler.sql` و`20260730030000_crawler_provider_usage_reports.sql` و`20260730040000_client_page_ai_link_profiles.sql` و`20260731010000_client_site_crawl_pgcrypto_fix.sql` و`20260731020000_economic_external_client_crawler.sql` بالترتيب قبل تحديث الواجهة والخادم. لا يحتاج هذا الترحيل إلى مفتاح ذكاء اصطناعي أو Search Console أو عملية PM2 جديدة؛ فالمفتاح مطلوب فقط عند تشغيل التوليد بعد الزحف. يبقى الزاحف المحلي متاحًا دون أي مفتاح، وتُضاف الخدمات الخارجية اختياريًا من **الإعدادات ← خدمات الزحف**.

يشغّل ملف PM2 العامل `bazarvan-staging-client-page-crawler` تلقائيًا؛ الاسم التاريخي غير المستخدم حاليًا هو `bazarvan-client-page-crawler`. يمكن ضبط سرعته وحدود الصفحات من متغيرات `CLIENT_PAGE_CRAWLER_*` الموثقة في `deploy/env.server.example`. لا يلزم إضافة مفتاح تشفير جديد؛ يشتق الخادم حماية مفاتيح الزحف من `SUPABASE_SERVICE_ROLE_KEY` الموجود أصلًا، ثم يدخل المسؤول مفاتيح Firecrawl وBrowserless من الواجهة. يسجل العامل كل محاولة في تقرير يومي مفصل دون تخزين المفتاح الخام.

عقد API وتشغيل الزاحف موثّقان في `docs/local-client-site-crawler-ar.md`.

## مفاتيح OpenAI وGemini المدفوعة التي يديرها المسؤول

قبل تفعيل المفاتيح المدفوعة التي يدخلها المسؤول من الإعدادات:

1. نفّذ `supabase/migrations/20260722050000_admin_ai_provider_secrets.sql` داخل **Supabase SQL Editor** إذا لم يكن منفذًا سابقًا.
2. نفّذ الترحيل الجديد `supabase/migrations/20260726000000_user_ai_provider_secrets.sql` مرة واحدة لإتاحة مفاتيح Gemini المجانية والمدفوعة وOpenAI المدفوعة لكل مستخدم.
3. نفّذ `supabase/migrations/20260823000000_content_writing_resume_secret.sql` مرة واحدة لإتاحة المفتاح المشفّر الخاص باستئناف كتابة المحتوى لكل مزود.
4. أنشئ مفتاح تشفير واحدًا على الخادم بالأمر `openssl rand -base64 32`.
5. أضف القيمة الناتجة باسم `AI_SETTINGS_ENCRYPTION_KEY` داخل `/var/www/bazarvan-editor-staging/.env.production`.
6. لا تحذف القيم الحالية لـ `OPENAI_API_KEY` و`GEMINI_PAID_API_KEYS`؛ فهي تبقى مفاتيح هوستينجر الاحتياطية عند فشل مفاتيح المستخدم ثم مفاتيح المسؤول.

تُشفّر مفاتيح المسؤول والمستخدمين الخام بخوارزمية AES-256-GCM قبل تخزينها، ولا تعيدها واجهة الإعدادات، ولا تستطيع أدوار Supabase من نوع `anon` أو `authenticated` قراءتها. في جلسة مستأنفة فقط يُجرّب مفتاح الاستئناف الخاص بالمزود أولًا، ثم مفاتيح المستخدم، ثم مفاتيح المسؤول، ثم مفاتيح هوستينجر. وفي بقية الطلبات يبقى الترتيب المعتاد دون تغيير.

فقدان `AI_SETTINGS_ENCRYPTION_KEY` أو تغييره يجعل مفاتيح المسؤول والمستخدمين المخزنة سابقًا غير قابلة للقراءة. احتفظ به داخل بيئة الخادم فقط، وأنشئ له نسخة احتياطية آمنة.

## ترحيل تحليل المنافسين المستقل

قبل نشر مسار **أفكار جديدة/متضاربة مع المنافسين** الذي يحلل كل منافس في طلب مستقل ثم ينفذ تجميعًا نهائيًا بالذكاء الاصطناعي، نفّذ هذا الترحيل مرة واحدة داخل **Supabase SQL Editor**:

`supabase/migrations/20260726020000_independent_competitor_engineering_analysis.sql`

ينشئ الترحيل تخزينًا مؤقتًا آمنًا لنتيجة كل منافس، ويحدّث قائمة الأوامر النشطة، ويلغي المهام المنتظرة للأوامر المتقاعدة مع الحفاظ على التقارير التاريخية المكتملة. نفّذ الترحيل قبل إعادة تحميل عمليات PM2 حتى لا يعمل الإصدار الجديد من دون التخزين المؤقت وقائمة الأوامر المحدثة.

إذا كان الترحيل السابق مطبقًا بالفعل، نفّذ بعده الترحيل التالي لإزالة «مقارنة محتوى المنافسين» و«تجميعة أوامر» من سجل القوالب النشط، وتسمية الأمر الباقي «تحليل المنافسين الشامل»:

`supabase/migrations/20260726030000_comprehensive_competitor_command.sql`

### استئناف تحليل المنافسين الشامل

بعد الترحيلين السابقين، نفّذ الترحيل التالي مرة واحدة قبل نشر الخادم والواجهة المحدثين:

`supabase/migrations/20260726040000_competitor_analysis_resume_controls.sql`

يضيف هذا الترحيل زر **استئناف الآن** للمهمة التي تنتظر موعد إعادة المحاولة. يبقى الاستئناف التلقائي مجدولًا حسب المدة الموجودة في **إعدادات المسؤول**، ولا يحذف الاستئناف اليدوي نتائج المنافسين المكتملة؛ يعيد العامل استخدامها ويكمل من أول منافس غير مكتمل. تعرض الواجهة نتيجة كل مقارنة مستقلة فور حفظها، ثم تعرض نتيجة الدمج النهائي كبطاقات قابلة للتطبيق في المحرر فقط.

## استيراد المقالة من رابط

قبل نشر زر **استيراد مقالة من رابط** نفّذ الترحيل التالي مرة واحدة داخل Supabase SQL Editor:

`supabase/migrations/20260731030000_article_url_import_source.sql`

يصنّف الترحيل المقالة الجديدة أو المستبدلة عبر الرابط بقيمة `source = 'import'`، ويحفظ رابط المصدر وبصمة المحتوى داخل بيانات المقالة. لا يحتاج السحب إلى مفتاح ذكاء اصطناعي أو Firecrawl، ولا ينزّل الصور.

يمكن إبقاء حدود السحب الافتراضية، أو ضبطها في `.env.production`:

```text
ARTICLE_IMPORT_TIMEOUT_MS=30000
ARTICLE_IMPORT_MAX_BYTES=4194304
```

## أوامر النشر

### سياسة النشر اليدوي المعتمدة

النشر من GitHub إلى Hostinger **يدوي فقط**. لا يوجد Workflow للنشر في GitHub
Actions، ولا Cron أو Webhook أو فحص دوري على Hostinger. بعد كل تعديل مكتمل تُرفع
النسخة إلى فرع `main`، ثم يُنفذ السحب والبناء وإعادة التشغيل يدويًا من Terminal
الخادم. يبقى Workflow **Quality gates** للفحص فقط ولا يملك صلاحية نشر.

يمكن استخدام `deploy/deploy-hostinger-production.sh` لتنفيذ الخطوات المحمية؛
فهو يقفل النشر المتزامن، ويتحقق من الالتزام المطلوب ومن نظافة نسخة الخادم ووجود
عمليات PM2، ولا يعيد تشغيلها قبل نجاح البناء، ثم يفحص `/healthz` و`/readyz`.

### خطوات النشر اليدوي

بعد تنفيذ ترحيلات Supabase المطلوبة، اتصل بالخادم عبر SSH ونفّذ:

```bash
cd /var/www/bazarvan-editor-staging
git pull --ff-only origin main
set -a
source .env.production
set +a
npm ci --include=dev
npm run build
for app in \
  bazarvan-editor-staging \
  bazarvan-staging-competitor-worker \
  bazarvan-staging-ai-worker \
  bazarvan-staging-full-article-pipeline-worker \
  bazarvan-staging-content-writing-preparation-worker \
  bazarvan-staging-ai-job-worker \
  bazarvan-staging-content-writing-worker \
  bazarvan-staging-client-page-crawler; do
  pm2 restart "$app" --update-env
done
pm2 save
curl -fsS https://smarteditor.bazarvan.com/healthz
curl -fsS https://smarteditor.bazarvan.com/readyz
```

معنى الأوامر باختصار:

- `git pull --ff-only origin main`: يسحب آخر إصدار مرفوع إلى GitHub من دون إنشاء دمج تلقائي.
- `source .env.production`: يحمّل متغيرات بيئة الإنتاج أثناء البناء وإعادة التشغيل.
- `npm ci --include=dev`: يثبت الاعتماديات طبقًا لملف القفل، بما فيها أدوات البناء
  مثل Vite حتى مع تحميل `NODE_ENV=production` من ملف البيئة.
- `npm run build`: يبني الواجهة والخادم والعوامل وينفّذ فحوص الإصدار.
- حلقة `pm2 restart`: تعيد تشغيل عمليات النسخة الذاتية الثماني فقط مع متغيرات البيئة المحدثة؛ لا تستخدم `pm2 restart all`. ينشئ سكربت النشر عامل تجهيز منافسي كتابة المقالة تلقائيًا في أول نشر، ثم يعيد تشغيله في النشرات اللاحقة.
- `pm2 save`: يحفظ قائمة العمليات لكي تعود بعد إعادة تشغيل الخادم.
- `/healthz`: يتحقق من أن خادم الويب يعمل.
- `/readyz`: يتحقق أيضًا من بناء الإنتاج، ومخططات Supabase المطلوبة، ومفتاح التشفير. يعرض حالة عامل المنافسين داخل `checks.externalAnalysisWorker` ويضع `degraded: true` عند تعطل طابوره، لكنه لا يعيد HTTP 503 بسبب تأخر مهمة وحده حتى لا تدخل مراقبة هوستينجر وPM2 في حلقة إعادة تشغيل تقطع المهام.

## ملاحظات وتشخيص المشكلات

- يشغّل PM2 خادم الويب والعمال السبعة الذاتيين من `/var/www/bazarvan-editor-staging`. هذا هو المسار المعتمد.
- أسماء العمليات الحالية تبدأ بـ`bazarvan-editor-staging` و`bazarvan-staging-*`؛ لا تُعد تشغيل الأسماء القديمة.
- لا تستخدم `/var/www/bazarvan-smarteditor` في تعليمات النشر المستقبلية إلا إذا أُعيد ضبط PM2 عمدًا للعمل منه.
- إذا لم تكن حالة النشر واضحة، اعرض جميع العمليات بالأمر `pm2 status`.
- لفحص خادم الويب استخدم `pm2 describe bazarvan-editor-staging`.
- لفحص عامل البحث وسحب المنافسين المستقل عبر Firecrawl استخدم `pm2 describe bazarvan-staging-competitor-worker`، ولسجله استخدم `pm2 logs bazarvan-staging-competitor-worker --lines 100`.
- يستخدم `bazarvan-staging-ai-worker` فقط للتحليل الدلالي والأوامر الهندسية، لذلك لا تمنع أخطاء Gemini عامل Firecrawl من استلام مهام المنافسين.
- إذا كان `/readyz` يعرض `degraded: true` و`checks.externalAnalysisWorker.ok: false` فخادم الويب جاهز، لكن ميزات البحث والسحب متدهورة ويجب فحص `bazarvan-staging-competitor-worker`. لا تعِد تشغيل خادم الويب تلقائيًا بسبب هذا التنبيه وحده.
- لفحص عامل كتابة المقالة استخدم `pm2 describe bazarvan-staging-content-writing-worker`.
- لفحص عامل تجهيز المنافسين قبل كتابة المقالة استخدم `pm2 describe bazarvan-staging-content-writing-preparation-worker`.
- لفحص عامل زحف صفحات العملاء استخدم `pm2 describe bazarvan-staging-client-page-crawler`.
- عند فشل `/readyz` لا تعتبر النشر مكتملًا؛ راجع الترحيلات ومتغيرات البيئة وسجل العملية في PM2.
